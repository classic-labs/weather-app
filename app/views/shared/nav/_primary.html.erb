<nav class="controls">
	<% if user_signed_in? %>
		<span class="control"><%= link_to "Dashboard", root_path, class: "control" %></span>
		<span class="control"><%= link_to "Logout", destroy_user_session_path, class: "control", data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to log out?" } %></span>
	<% else %>
	  <span class="control"><%= link_to 'Login', new_user_session_path, class: "control" %></span>
	<% end %>
</nav>
